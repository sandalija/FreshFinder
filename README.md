# FreshFinder

Sólo requiere de un servidor XAMPP y conectarse a [localhost](localhost).

Configura el direcorio del proyecto en htppd.conf en C:\xampp\apache\conf
````bash
DocumentRoot "C:\FreshFinder"
<Directory "C:\FreshFinder">
````